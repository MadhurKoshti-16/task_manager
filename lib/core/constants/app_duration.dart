abstract final class AppDurations {
  AppDurations._();

  static const Duration splashDuration = Duration(milliseconds: 3600);

  static const Duration pageTransitionDuration = Duration(milliseconds: 450);
  static const Duration reversePageTransitionDuration = Duration(
    milliseconds: 350,
  );
}
